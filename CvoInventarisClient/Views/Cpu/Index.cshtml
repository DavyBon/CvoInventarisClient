@model IEnumerable<CvoInventarisClient.Models.CpuModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{ 
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdCpu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Merk)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Snelheid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FabrieksNummer)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCpu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Merk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Snelheid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FabrieksNummer)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdCpu }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdCpu }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdCpu }) 
            </td>
        </tr>
    }
</table>
